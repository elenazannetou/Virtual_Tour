<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Virtual Tour (Fullscreen)</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <style>
    html, body, iframe { margin:0; padding:0; width:100%; height:100%; border:0; }
    #gate {
      position: fixed; inset: 0;
      display: grid; place-items: center;
      font-family: system-ui, Arial, sans-serif;
      background: #000; color: #fff;
      text-align: center; padding: 2rem;
    }
    button {
      font-size: 18px; padding: 12px 20px; border: 0; border-radius: 8px; cursor: pointer;
    }
    #gate.hidden { display: none; }
  </style>
</head>
<body>
  <div id="gate">
    <h1 style="margin:0 0 12px 0;">Virtual Tour</h1>
    <p style="margin:0 0 20px 0;">Click the button to enter fullscreen.</p>
    <button id="enterBtn">Enter Tour</button>
  </div>

  <iframe id="tour" src="" allowfullscreen referrerpolicy="no-referrer"></iframe>

  <script>
    // 👇 Use your /present link here (yours from earlier)
    const TOUR_URL = "https://cloud.chaos.com/collaboration/virtual-tour/NeEiaZ3LyBTPZbTaqtjFn7/present?n=6WqSa59A1QRHW4sVrfjBdH";

    const iframe = document.getElementById('tour');
    const gate = document.getElementById('gate');
    const btn  = document.getElementById('enterBtn');

    function enter() {
      if (!iframe.src) iframe.src = TOUR_URL;
      const el = iframe.requestFullscreen ? iframe : document.documentElement;
      (el.requestFullscreen || el.webkitRequestFullscreen || el.msRequestFullscreen || (()=>Prom
